let (* EXPORT BEGIN*)
    fun measure_time f = 
        let val time_start = getTime()
        in
            f ();
            (getTime() - time_start)
        end

    fun progress_cps cps = case getType cps of
    "function" => progress_cps (cps ())
    | x => cps
    (* EXPORT END*)
in ()
end