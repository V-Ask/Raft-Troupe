(* EXPORT START *)

fun cps_test1 () = 
    let val raft = raft 5 (simpler_cps ()) (fn x => x.entries_since_snap > 2) false
    in 
        send(raft, ("SEND-TO-NTH", 1, "debug-timeout"));
        send_delay(raft, ("RAFT-UPDATE", "This is a message")) 500;
        send_delay(raft, ("SEND-TO-ALL", "debug-printlog")) 5000
end

(* EXPORT END *)