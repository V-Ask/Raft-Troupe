import lists
datatype Atoms = WAIT|SUS|DONE

let (* EXPORT START *)

    fun main x = case x of
        ("ping", x) => sleep 25; ([(RAFT_UPDATE, ("pong", x))], WAIT, main)
        |_ => ([], WAIT, main)
  
    fun ping_server () =
        ([], WAIT, main)
  (* EXPORT END *)
in ()
end
