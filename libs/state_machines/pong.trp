datatype Atoms = WAIT|SUS|DONE

let (* EXPORT START *)
    fun main dialer x = case x of
    (callback, "pong", x) => sleep 5; ([(RAFT_UPDATE, ("ping", x + 1))], WAIT, main)
    |_ => ([], WAIT, main)

    fun pong_server cluster =
        ([], WAIT, main (raft_dial cluster (self())))

    (* EXPORT END *)
in ()
end